# Spec Index — __PROJECT_NAME__

Every spec lives under `specs/<domain>/`. `<domain>` MUST match a domain in
`.basalt/manifest.decree`. Run `quarry check` to verify correlation.

| Domain | Spec | Status | Decisions |
|--------|------|--------|-----------|
| _example_domain | _example_domain/spec.md | draft | — |

> Replace the row above. Status: `draft` | `ready` | `done`.
> Decisions: comma-separated references to `decisions.log` entries
> (e.g. `order_processing/Order.total`).